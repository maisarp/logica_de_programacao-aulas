#include    <stdio.h>
#include    <stdlib.h>

main ()
{
    int A, B, Soma;
    printf("Digite o valor de A:"); 
    scanf("%d", &A);
    printf("Digite o valor de B:");
    scanf("%d", &B);
    Soma = A + B;
    printf("A soma de A + B = %d\n", Soma);
    
}